export class Rect {
  top: number;
  left: number;
  width: number;
  height: number;

  constructor({ top, left, width, height }) {
    this.top = top;
    this.left = left;
    this.width = width;
    this.height = height;
  }
}
